# OnePolygonPerAsset
This repository is to take asset point lines and polygon features ands dissolve on asset ID and buffer points and lines to provide 1 polygon per asset.  This will then overwrite a feature class nightly in AGOL.
